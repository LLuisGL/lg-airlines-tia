application:
  security:
    jwt:
      secret-key: 2u8Chpo73eFHahZkh9CkyNgZvAAn4efrGDGTsNcgJxer8esjsFD9orDMZpukgU2E
      expiration: 86400000
      refresh-token:
        expiration: 604800000
spring:
  application:
    name: user-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST:localhost:5432}/${DB_NAME:dbair_tia}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:admin}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false
server:
  port: ${SERVER_PORT:8087}
